# Dependencies
node_modules/
package-lock.json
.pnpm-store/

# Datasets: reference material is generated LOCALLY per category by the
# dataset-builder agent and is NOT committed (third-party site names, URLs,
# screenshots). Run the agent to build the datasets you need.
/data/datasets/

# OS / editor cruft
.DS_Store
Thumbs.db
desktop.ini
*.swp
.idea/
.vscode/

# Logs / temp
*.log
tmp/
.tmp/
*.tmp
.playwright-mcp/

# Local Claude settings (keep shared settings, ignore machine-local)
.claude/settings.local.json

# Secrets
.env
.env.local
*.key

# Stray QA screenshots: skill temp renders belong in tmp/qa/ (already ignored
# via tmp/); safety net for PNGs mistakenly saved in the repo root
/*.png
