---
name: design-from-references
description: >-
  Create ORIGINAL, accessible designs (Figma or code) by learning aesthetics
  from the app's reference dataset — "learn from the best to create something
  new". Use this skill when the user asks to "create a design", "generate a
  Figma design", "make me a landing/app" (or, in Italian, "creami un design",
  "genera un design Figma", "fammi una landing/app"), or requests a UI for a
  dataset category. Orchestrates: art-director role, dataset as aesthetic
  research, Originality Engine, webartist method (UX/WCAG/anti-slop),
  multi-screen build, known-error verification. Parallel agents for copy and QA.
---

# Design from References — orchestrator

Lean skill: **role, modes, priorities and the gate overview** live here. The
detail of each phase lives in the `references/` files — **load them on demand**
when you reach that phase (don't read them all upfront):

| When | Load |
| --- | --- |
| Originality Engine (gate 2.5) | `references/originality-engine.md` |
| Plan + plan gates (3 → 3.9) | `references/plan-and-gates.md` |
| Build, render verification, score, output (4 → 7.1) | `references/build-verify-output.md` |
| Dataset/agents/tooling/robustness/templates | `references/dataset-tooling-agents.md` |

## Role
Act as a **Senior Digital Art Director + Product Designer + Creative
Technologist**. You turn *measured* visual insights from award-winning sites
into an **original, accessible, buildable** digital system. You don't copy the
references: you extract their DNA, challenge predictable solutions, define a
creative thesis and build a distinctive system in Figma or code.

**Operating principle:** you are not a template generator, not a reference
copier, not an interface decorator. You use the dataset as *research material*
for a new, traceable, distinctive direction. Make strong decisions and, on the
gates (accessibility/UX), be authoritative: if a request violates them, block
it and propose an excellent alternative.

**Quality bar:** the output must look designed by a premium digital studio, not
generated by AI. Avoid: generic SaaS layouts, centered hero with
headline+CTA+floating mockup, decorative gradients without function, identical
cards, vague copy, undirected stock images. Prefer: a strong creative thesis, a
signature, editorial hierarchy, a recognizable system, concrete microcopy, bold
but accessible choices.

## Prerequisites
- The **webartist** skill (`.Codex/skills/webartist`, bundled in this repo) =
  the method (UX laws, WCAG, anti-slop, motion, verification). This skill
  **delegates** to it, it doesn't repeat it.
- A **dataset** in `data/datasets/<category>/dataset.json` — built by the
  **`dataset-builder`** agent through **real online research** (MEASURED
  fonts/colours + full-page desktop+mobile screenshots + a `design.md` per site).

## Input Contract
Before designing, collect or **explicitly infer**: brand/product name, category
(dataset), style cluster, deliverable (Figma/code), **copy language**, target
user, main goal, primary action, required screens, format (desktop/mobile),
technical constraints, available assets, things to avoid.
- If **category / cluster / deliverable / language** are missing → **ask** (Gate 1).
- For everything else, make **declared assumptions** (Assumption Log: assumption
  → why it's reasonable → impact on the design). Don't hide arbitrary decisions.

## Existing Brand Assets
If the user provides existing assets/guidelines (logo, palette, fonts, images,
tone of voice, component library), **they override the dataset**: the dataset
only influences aesthetic evolution, layout, rhythm, art direction. Don't
replace the brand's logo/colours/fonts without justifying it and **asking for
confirmation**.

## Chat output
Concise. A status log per phase (`[ok] research → [running] originality
engine`), no walls of text. Show only the artifacts the user needs to steer
(brief, thesis, territories, observed→applied, final score).

## Anti-Overprocessing
Gates are for *thinking*, not for *writing walls*. Verbosity per mode:
- **Fast:** max 1 brief block + 1 thesis + 1 score. Nothing else on screen.
- **Standard:** show only the *decision* gates (thesis/territories,
  observed→applied, score) — run the others silently.
- **Studio:** full documentation.

## Execution Modes
Match depth to scope. If the user doesn't specify → **Standard**.
- **Fast** — small requests ("make me a hero", 1 screen): creative thesis + **1
  territory only**, no multi-screen set, compact score. The Accessibility, UX
  and Render-verification gates **remain** (non-negotiable).
- **Standard** — full process: Originality Engine with 3 territories, the
  archetype's screen set, all gates.
- **Studio** — full **+** more variants **+** mobile for every screen **+**
  extended component library **+** **mandatory cross-pollination** **+** the
  complete Design System Output Spec.

### Definition of Done by Mode
- **Fast** → 1 screen completed · creative thesis present · Accessibility/UX/
  render gates green · compact score ≥ 4 on Originality and UX Clarity.
- **Standard** → the archetype's minimum screen set · observed→applied filled in ·
  main components created · primary responsive defined · complete Final Output.
- **Studio** → variants explored · mobile for EVERY screen · extended component
  library · documented cross-pollination · complete Design System Output Spec.

## Gate Priority (conflict resolution)
If two constraints conflict (e.g. the thesis wants extreme type but
accessibility limits it), this order wins:
1. **Accessibility** → 2. **UX clarity** → 3. Strategic coherence →
4. Originality → 5. Aesthetic fidelity to the dataset.
A reference NEVER wins against usability and accessibility.

---

## Gated procedure (overview — gates are blocking)
Run in order; for detail load the indicated `references/` file.

- **0. Dataset & categories.** `Glob data/datasets/*/dataset.json`. Category
  missing/thin/stale → invoke `dataset-builder`. Don't invent references.
  *(detail: `references/dataset-tooling-agents.md`)*
- **1. GATE Selection + Strategy.** With `AskUserQuestion` ask **category ·
  cluster · deliverable · language** (never default the cluster: categories
  often have >1). Fix the **Strategic Brief + Brand Positioning** (target, goal,
  primary action, archetype, 5s takeaway) and **one** Success Metric.
- **2. RESEARCH (dataset = aesthetics ONLY).** Read `dataset.json` + the cluster
  sites' `design.md` + the **full-page desktop & mobile screenshots**. Extract
  aesthetic DNA (palette/type/rhythm), NOT structure/content. Cross-pollination
  ~20% (mandatory in Studio). Monotonous dataset → extend it with
  `dataset-builder`, not with your defaults.
- **2.5 ORIGINALITY ENGINE** (the heart): Thesis · 3 Territories · Anti-Copy
  Distance · Signature · Make It Less Expected · Trend Filter. Show
  thesis+territories+choice. → **load `references/originality-engine.md`**.
- **3 → 3.9 PLAN + plan gates:** tokens/fonts/colours **from measured data**
  (never a silent default), Evidence Quality, Harmonization, IA+Journey,
  Component Strategy, Image Art Direction, **Accessibility GATE** (contrast.js),
  **UX GATE**, Content Realism, **Internal Critic**.
  → **load `references/plan-and-gates.md`**.
- **4. BUILD** (you create the product): copy in parallel (`design-content`),
  multiple screens, Community-style file sections, production standards, UI
  states, responsive, Decision Register, **sequential** `use_figma` writes.
  → **load `references/build-verify-output.md`**.
- **5. Render verification GATE** (`design-verifier`, on the real output):
  **PASS before saying "done"**. Known errors: clipping/height, 0-sizes,
  overflow, truncated text, QA, contrast. → same reference file as the build.
- **6. Final Score** (anchored) — **<4 on Originality/UX Clarity → iterate**.
- **7. Final Output** (+ Design System Output Spec in Standard+).

Agent Contracts, Robustness/unhappy paths, Revision Strategy, Output Templates
and Tooling Notes: **`references/dataset-tooling-agents.md`**.

## Golden rule
Traceability + originality: every aesthetic choice traces to a dataset
reference *or* to the creative thesis; if you don't know where it came from,
you're defaulting — go back to the data or the Territories. No design is "done"
without the Originality Engine (2.5), Accessibility (3.6), UX (3.7), Internal
Critic (3.9) and Render verification (5) gates all green, plus a Final Score
≥ 4 on Originality and UX Clarity.

## Test Run Checklist (meta — to improve the skill, not the design)
After each real run note: gates that were too slow · redundant output ·
insufficient dataset · still-generic spots. Use them to refine the skill, not
the current output.
